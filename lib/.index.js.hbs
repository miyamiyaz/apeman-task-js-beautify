/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";

var define = require('./define'),
    pkg = require('../package.json');

var lib = define.bind(this);
lib.define = define;
lib.version = pkg.version;

module.exports = lib;
